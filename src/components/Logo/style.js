import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    containerLogo: {
        marginBottom: 40,
        alignItems: "center",
    },
    logo: {
        width: 260,
        height: 100,
    },
});

export default styles